# API Configuration Environment Variables
# Set CIVIC_API_ENV to control which endpoint configuration to use
# Works on both Android and Desktop platforms

# Options: production, development, local_network
# Default: development (desktop), production (Android)
CIVIC_API_ENV=local_network

# Examples:
# CIVIC_API_ENV=production       # Uses civicbridge.duckdns.org endpoints
# CIVIC_API_ENV=development      # Uses localhost:5000 endpoints  
# CIVIC_API_ENV=local_network    # Uses 10.42.19.207:5000 endpoints

# Note: Firebase service account key is NOT needed here
# It's only required on the Flask server side for token verification